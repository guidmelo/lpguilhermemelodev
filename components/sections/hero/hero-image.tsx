// ProfileImage is the source of truth — this file re-exports it as HeroImage
// so the hero-section import chain stays unchanged.
export { ProfileImage as HeroImage } from '@/components/hero/profile-image'
